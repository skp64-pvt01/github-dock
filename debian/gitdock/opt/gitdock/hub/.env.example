# GitDock Hub - copy to .env and fill in
# Required for signing session cookies (use a long random string, min 32 chars)
HUB_SECRET=your-long-random-secret-at-least-32-chars
# Optional: encrypt database at rest (min 32 chars). If unset, DB runs unencrypted (e.g. local dev).
HUB_DB_KEY=
# Port
PORT=3848
# Lemon Squeezy (hosted Hub only - optional for self-host)
LEMONSQUEEZY_WEBHOOK_SECRET=
LEMONSQUEEZY_CHECKOUT_URL=
